#include<stdio.h>
int main(int argc, char const *argv[])
{
    int num;   //wnere num is any number
    int r=1,i;
    printf("enter any no=");
    scanf("%d",&num);
    i=num;
    while(i>=1)
    {
    r=r*i;
    i--;
    }
    printf("fact of %d is %d",num,r);
    return 0;
}
